# ╔══════════════════════════════════════════════════════════════════════╗
# ║  DIXPORD CONFIGURATION FILE                                        ║
# ║  Rename this file from ".env.example" to ".env" and fill in below  ║
# ╚══════════════════════════════════════════════════════════════════════╝

# ─────────────────────────────────────────────────────────────────────────
# DISCORD USER TOKEN  (RECOMMENDED — gives full access to YOUR messages)
# ─────────────────────────────────────────────────────────────────────────
# This is YOUR personal Discord auth token. It lets the tool access
# everything YOU can see: all your DMs, servers, channels, messages.
#
# How to get it:
#   1. Open Discord in your BROWSER (not the desktop app)
#      → Go to https://discord.com/app
#   2. Press F12 to open Developer Tools
#   3. Go to the "Network" tab
#   4. In the filter box, type "api" to narrow results
#   5. Click on any request to the Discord API
#   6. In the "Headers" section, find "Authorization"
#   7. Copy that value — that's your token
#
# ⚠️  KEEP THIS SECRET! Anyone with this token can access your account.
# ⚠️  Never share it, never commit it to git, never post it online.
# ⚠️  If compromised, change your Discord password immediately.
#
DISCORD_USER_TOKEN=YOUR_USER_TOKEN_HERE


# ─────────────────────────────────────────────────────────────────────────
# DISCORD BOT TOKEN  (ALTERNATIVE — limited, cannot see personal DMs)
# ─────────────────────────────────────────────────────────────────────────
# This is a Discord Bot token. Bots can only see server channels they
# have access to and DMs sent directly to the bot.
# You only need this if you don't want to use the user token above.
#
# How to get it:
#   1. Go to https://discord.com/developers/applications
#   2. Create an application → go to "Bot" tab → click "Reset Token" → copy it
#   3. Paste it below (no quotes needed)
#
DISCORD_BOT_TOKEN=YOUR_BOT_TOKEN_HERE


# ─────────────────────────────────────────────────────────────────────────
# GITHUB CREDENTIALS  (OPTIONAL — only needed if you plan to contribute)
# ─────────────────────────────────────────────────────────────────────────
# Your GitHub username
GITHUB_USER_NAME=YOUR_GITHUB_USERNAME_HERE

# Your GitHub Personal Access Token
# How to get it:
#   1. Go to https://github.com/settings/tokens
#   2. Click "Generate new token (classic)"
#   3. Check the boxes you need, set expiration, click "Generate token"
#   4. Copy and paste below
#
GITHUB_TOKEN=YOUR_GITHUB_TOKEN_HERE


# ─────────────────────────────────────────────────────────────────────────
# DISCORD LOGIN CREDENTIALS  (OPTIONAL — for reference only)
# ─────────────────────────────────────────────────────────────────────────
# These are your personal Discord login email/password.
# The tool does NOT use these — it uses the Bot Token above.
# You can store them here for your own reference if you like.
#
DISCORD_USER_NAME=your_discord_email@example.com
DISCORD_USER_PASSWORD=your_discord_password_here


# ─────────────────────────────────────────────────────────────────────────
# EXPORT SETTINGS  (OPTIONAL — defaults are fine for most people)
# ─────────────────────────────────────────────────────────────────────────

# Where to save exported files (default: ./exports)
EXPORT_DIR=./exports

# Default export format when you don't specify one: txt, md, or pdf
DEFAULT_FORMAT=txt

# Max messages the bot fetches per API request (max allowed by Discord: 100)
FETCH_LIMIT=100
